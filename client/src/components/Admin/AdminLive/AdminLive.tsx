import React from 'react';
import { Outlet } from 'react-router-dom';

const AdminLiveBox: React.FC = (props) => {
  return (
    <div id="live">
      yeetus?
      {/* <Outlet /> */}
    </div>
  );
};

export default AdminLiveBox;
