import React from 'react';

export interface IAccountPageProps {}

const AccountPage: React.FC<IAccountPageProps> = (props) => {
    return (
        <div>
            <p>This is the Accountpage.</p>
        </div>
    );
};

export default AccountPage;
